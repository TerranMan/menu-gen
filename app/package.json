{
  "name": "menu-gen",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Генератор семейного меню на неделю",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-data.mjs && vite build",
    "preview": "vite preview",
    "build:data": "node scripts/build-data.mjs",
    "migrate": "node scripts/migrate-legacy.mjs",
    "fetch:images": "node scripts/fetch-images.mjs",
    "fetch:prices": "node scripts/fetch-prices.mjs"
  },
  "dependencies": {
    "alpinejs": "^3.14.8"
  },
  "devDependencies": {
    "cyrillic-to-translit-js": "^3.2.1",
    "vite": "^5.4.10",
    "yaml": "^2.6.1"
  }
}
